#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vscode-json-languageserver/bin/vscode-json-languageserver" "$@"
  ret=$?
else 
  node  "$basedir/../vscode-json-languageserver/bin/vscode-json-languageserver" "$@"
  ret=$?
fi
exit $ret
